spring:
  datasource:
    username: postgres
    password: postgres301946
    url: jdbc:postgresql://localhost:5432/workplace


  jpa:
    database: POSTGRESQL
    generate-ddl: true
    hibernate.ddl-auto: update
    show-sql: true
    properties.hibernate.temp.use_jdbc_metadata_defaults: false